# 文字转语音台词

相信大家都在某现代商业操作系统上遇到过「应用程序错误」的弹窗。当程序在错误的内存地址进行读写操作时，弹窗会告诉用户，该内存不能为 read 或不能为 written。甚至当你安装了调试器后，还可以选择按「取消」来调试此程序。

世界上使用最广泛的某现代商业操作系统从约 20 年前就在使用这种错误弹窗来直观地提示用户发生错误的指令地址和内存地址，而 Linux 却只是在程序运行出现问题时弹出一个 Crash Reporting System，并在终端内提示短短的一行 core dumped，非常不优雅。为什么不扩大 Linux 的市场呢？

于是我修复了 Linux 上没有「应用程序错误」窗口的 bug，这样 Linux 用户在程序遇到问题时也可以看到弹窗了。

于是我发明了：

---

libschrodinger。

libschrodinger 在程序触发部分致命错误信号时拦截，弹出一个 Windows 风格的错误对话框，以还原自 Windows XP 时代流传下来的神秘提示。弹窗上包含 2 个按钮，单击「确定」将恢复之前的信号处置流程，并使得错误进程按照之前的路径终止；单击「取消」则会使用调试器直接附加到该进程，这里优先使用 pwndbg，但可能回退到 gdb。例如，我们在这里点击「取消」，打开了 pwndbg；你可以在这里看到 backtrace，切换栈帧后便可以看到具体崩溃情况。

同前作 libobscure，你仍然需要将 libschrodinger 以 LD_PRELOAD 加载。

你可以使用 Python 来触发错误。例如，这里我们触发一次 Signal Segmentation Violation。

以往 Windows 上的错误弹窗仅标注了该内存不能为 read 或者 written。我们扩展了报错弹窗的类型：libschrodinger 支持拦截 Signal Segmentation Violation 的非法读访问和写访问、Signal Bus 的总线错误、Signal Ill 的非法指令错误、Signal Abort 的非正常终止错误。

更进一步地，我们还添加了除零错误、整数溢出错误、浮点除法错误、浮点溢出错误、浮点下溢错误、无效浮点运算错误、浮点下标越界错误和算数异常错误。在 tests 文件夹下有大多数使用例。对于其中的大多数错误，你都可以点击「取消」以附加调试器。

项目在 GitHub 上以 MIT License 开源。

---

还有，感谢以上两位主要开发者，绝大部分的贡献都是他们做的。

视频制作不易，恳请大家点赞关注，或者给项目一个 Star，让更多人吃上错误代码。
